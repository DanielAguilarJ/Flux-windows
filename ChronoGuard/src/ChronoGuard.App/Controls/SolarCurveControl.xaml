<UserControl x:Class="ChronoGuard.App.Controls.SolarCurveControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="600">
    <Grid>
        <!-- Background gradient for day/night visualization -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Rectangle>
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,1" EndPoint="0,0">
                        <GradientStop Color="#0D000000" Offset="0"/>
                        <GradientStop Color="#1A000000" Offset="0.5"/>
                        <GradientStop Color="#0D000000" Offset="1"/>
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>
        </Grid>

        <!-- Canvas for curve and sun glyph -->
        <Canvas x:Name="PART_Canvas" Background="Transparent"/>
    </Grid>
</UserControl>
