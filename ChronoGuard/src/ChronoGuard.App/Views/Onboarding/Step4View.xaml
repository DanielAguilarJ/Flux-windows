<UserControl x:Class="ChronoGuard.App.Views.Onboarding.Step4View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="420">
            <TextBlock Text="Configuración final" FontSize="22" FontWeight="Bold" Margin="0,0,0,16"/>
            <CheckBox Content="Iniciar automáticamente con Windows" Margin="0,0,0,8"/>
            <CheckBox Content="Habilitar notificaciones" Margin="0,0,0,8"/>
            <ComboBox Width="200" Margin="0,0,0,8">
                <ComboBoxItem Content="Notificaciones básicas"/>
                <ComboBoxItem Content="Notificaciones completas"/>
                <ComboBoxItem Content="Modo silencioso"/>
            </ComboBox>
            <TextBlock Text="Resumen de configuración elegida" Margin="0,12,0,0"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
                <Button Content="Atrás" Command="{Binding BackCommand}" Width="100" Margin="0,0,8,0"/>
                <Button Content="Finalizar" Command="{Binding FinishCommand}" Width="120"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
